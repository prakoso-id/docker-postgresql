# TYPE  DATABASE        USER            ADDRESS                 METHOD
# Allow any user from any host to connect to any database with password authentication
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5

# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
